import React from 'react';

function TicketEmail({email}) {
  return (
    <div>
      We've emailed your ticket to {email} and will send updates in
      the run up to the event.
    </div>
  );
}

export default TicketEmail;
